# URL del backend real (solo la usa el proxy server-side en app/api/[...path]/route.ts;
# el navegador nunca la ve — ya no hace falta NEXT_PUBLIC_API_URL).
# Producción (esta app, Vouchers): https://sistema-vouchers-backend-production.up.railway.app
BACKEND_URL=http://localhost:8000
